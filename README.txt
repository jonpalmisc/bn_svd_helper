This is a work-in-progress plugin developed while reversing some firmware on a
flight. It will create sections and structures using the information contained
in a SVD file.

This plugin depends on my `svdparse` library [1], which is not on PyPI, so you
will have to install it manually.

[1] <https://github.com/jonpalmisc/svdparse>
